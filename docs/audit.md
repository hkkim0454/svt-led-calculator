# AI-DLC Audit Log — LED Wall Configurator

## Initial User Request

**Timestamp**: 2026-07-23

**User Input**: 삼성 LED configurator처럼, 설치 공간 정보만 입력하면 모델별로 캐비닛을 최대한 채워 크기·해상도·무게·밝기·소비전력 등을 산출하는 웹 도구를 제작 요청. AIDLC 방식으로 진행하며 프롬프트 작성법 안내 요청.

**AI Response**: AIDLC 3단계(Inception/Construction/Operations) 프롬프트 가이드 제공 및 즉시 사용 가능한 작동 프로토타입(`led-configurator.html`) 생성. 이후 삼성 공식 도구를 직접 검증하고 첨부된 AI-DLC 템플릿(state/audit)을 프로젝트 기준으로 작성.

**Context**: INCEPTION 시작. Greenfield 프로젝트, 기존 코드 없음.

---

## Reverse-Engineering the Samsung Reference (Browser)

**Timestamp**: 2026-07-23

**User Input**: 삼성 configurator 사이트를 직접 조작해 확인하고, 첨부 AI-DLC 템플릿으로 작업 요청.

**AI Response**: 사용자 승인 하에 Chrome(Browser 1)으로 `display-configurator.biz.samsung.com` 접속. 조작 흐름(모델 선택 → 단위 → 폭/높이 → Columns/Rows 또는 Fit-to-wall → Screen Type/Resolution/S-Box Redundancy), 모델 라이브러리(69종), 산출 항목, BOM 구조 확인. MP012F를 6×3.4m 벽에 Fit-to-wall 실행하여 실제 산출값 확보.

**Context**: INCEPTION, Requirements Analysis 보강. 삼성 도구는 SPA(클라이언트 렌더링)라 정적 조회로는 불가 → 브라우저 조작으로 확인.

**Decision**: 산출 항목·데이터 스키마를 삼성 실측 기준으로 정렬(DEC-002). 평균전력 계수를 실측값 0.53으로 정정(DEC-003).

**Artifacts**: 브라우저 세션 스크린샷(사용자 로컬 저장), 본 감사 로그 및 aidlc-state.md 갱신.

**Approval Status**: 브라우저 접속 — 승인(2026-07-23, Browser 1 선택). 정보 수집 활동으로 별도 산출물 승인 불요.

**Verification**: 삼성 도구 산출값을 화면에서 직접 대조. MP012F/6×3.4m → 7×6=42캐비닛, 5.644×2.721m, 4480×2160, 386.4kg, 최대 6132W/평균 3234W 확인.

**Open Items**: Fit-to-wall 세로 충진 규칙이 순수 floor와 상이(6행 채택, 7행 가능) → Q1.

**Compliance**: 브라우저 사용 전 사용자에게 대상 브라우저 확인. 개인정보·자격증명 미수집. 삼성 사이트 조작은 조회·시뮬레이션 범위로 한정, 계정 생성/제출/구매 행위 없음.

---

## Prototype v0.1 (Code Generation — Preliminary)

**Timestamp**: 2026-07-23

**User Input**: "프로그램을 작성해줘" — 작동하는 configurator 요청.

**AI Response**: 단일 HTML 프로토타입 생성 — 공간 입력 기반 최대 충진 계산, 배열 시각 미리보기, 산출 readout(크기·해상도·대각·중량·밝기·최대/평균 전력·화면비·여백), 모델별 비교표, 모델 CRUD, JSON import/export, 스펙 정합성 경고, 인쇄/PDF.

**Context**: INCEPTION 단계에서의 선행 프로토타입(디자인 검증용). 정식 Construction 착수 전.

**Decision**: vanilla JS 단일 파일, 계산 엔진·모델 데이터·UI 분리 구조 채택(DEC-001).

**Artifacts**: `/outputs/led-configurator.html`

**Approval Status**: 대기 — 오너 리뷰 예정.

**Verification**: 계산 로직을 Node로 수동 실행하여 검증(충진·크기·해상도·중량·전력 산출 정상). 단, 삼성 세로 충진 규칙 차이로 결과 일부 불일치.

**Open Items**: 실측 스펙 데이터 교체(R1), 평균전력 계수 0.53 반영(P2), 세로 충진 규칙 확정(Q1), BOM/회로/발열 항목 추가(P4).

**Compliance**: 외부 의존성 0, 개인정보 미수집. 스펙 데이터는 "예시값"으로 명시하여 오용 방지.

---

## Comparison with DISPLAY FIT & Direction A (Owner-Approved)

**Timestamp**: 2026-07-23

**User Input**: 친구가 만든 유사 도구 DISPLAY FIT(ChatGPT 제작)를 공유하며 참고 요청. 이후 **방향 A 채택**(본 프로젝트 유지 + 좋은 아이디어 흡수)과 **feet 단위 미도입**(한국은 meter만 사용) 지시.

**AI Response**: 두 프로젝트를 계산식·데이터·기능으로 비교(`docs/comparison-displayfit.md`) 후 A안을 코드에 반영. (1) S-Box 산출 규칙 확정 `ceil(전체픽셀/컨트롤러용량)`·이중화 ×2, (2) `dataStatus` 3단계 도입, (3) 평균전력 모델별 실측 typical 우선, (4) MP008F 검증 전력(122W/64W) 반영. 단위는 mm/meter만 유지. 계산 테스트 6→10종으로 확장, 전부 통과.

**Verification**: S-Box 규칙을 삼성 실측과 대조 — MP012F 42캐비닛(4480×2160) ÷ SBB-CS4BPGS(3840×2160) = 2대로 삼성 도구 출력과 일치. `tests/engine.test.js` 10/10 green.

**Open Items**: Q1(세로 충진 규칙)·Q2(범위: 일체형/Video Wall 포함 여부) 여전히 미확정. 스페어 캐비닛·S-Box 반올림 규칙 확인 필요.

---

## Decision Log

| ID | Timestamp | Decision | Rationale | Owner | Status |
|---|---|---|---|---|---|
| DEC-061 | 2026-09-16 | **3D 뷰를 Three.js(WebGL)로 교체 — STEP 1: 최소 골격.** 오너 지시로 03 배열 미리보기의 3D 뷰를 Yealink Control Deploy Tool 계열의 interactive 3D workspace로 발전시키기 위한 단계적 재구축을 시작한다. STEP 1 범위는 **렌더러·씬·원근 카메라·OrbitControls·기본 조명·바닥·정면(LED) 벽·좌측 벽·우측 벽·LED 화면·무대**까지. 좌석·사람·치수·바닥 격자·포인트 벽·미니맵·PNG·시점 프리셋 UI는 다음 단계로 미룬다. **(1) 의존성** — Three.js 0.180.0을 `src/vendor/three/`에 **파일로 동봉**(CDN 아님). CLAUDE.md 규칙 4의 '빌드리스 유지'를 지키면서 사내망에서 외부 CDN이 막혀도 동작하게 하고, 버전을 저장소에 고정해 어제 만든 제안서 이미지와 오늘 것이 달라지지 않게 한다. 원본에서 고친 곳은 `OrbitControls.js` 12행의 `from 'three'` → `from './three.module.min.js'` **단 한 줄**(번들러가 없어 bare specifier를 해석할 수 없음). `npm test`는 여전히 Node 내장만 쓴다 — 동봉본은 브라우저 전용이라 테스트가 불러오지 않는다. **(2) 단위** — 계산기는 mm, Three.js는 1 unit = 1 m가 조명·카메라 기본값에 맞으므로 `gl-model.js`의 `MM_PER_UNIT = 1000` / `u(mm)` 한 곳에서만 환산한다. 씬 안에서는 mm를 쓰지 않는다. **(3) 파일 분리** — Three.js를 import하는 파일은 Node에서 불러올 수 없어 `npm test`로 검증이 불가능하다. 그래서 **데이터 연결 부분만** `src/gl-model.js`(순수, DOM·Three 없음)로 떼어 회귀 테스트 10건을 붙였다. 그리기만 `render3d-gl.js`. **(4) 카메라** — Reference A의 실내 시점. 기존 Canvas 뷰는 카메라가 방 '밖'에 서는 그림이라 화각 33°로 충분했지만, 실내 시점은 카메라가 뒷벽보다 뒤로 못 가므로 33°면 LED만 화면을 꽉 채운다. 화각 **40°**, 눈높이 2.35 m, 바라보는 높이 1.25 m(눈높이보다 낮아야 바닥이 화면에 들어온다), 거리는 방 크기가 아니라 **LED 크기**로 정하고 방 깊이로 제한(`viewDistance`). 벽을 한쪽 면만 그리므로 OrbitControls 방위각을 ±60°로 묶어 카메라가 방 밖으로 나가 벽이 사라지는 일을 막는다. **(5) 계산 무변경** — engine.js·room-presets.js·models.js는 한 줄도 고치지 않았고 3D는 `resolvePreviewTarget()` 결과를 **읽기만** 한다. `renderPreview()`(정면 뷰) 327줄은 main과 바이트 단위로 동일하며, `styles.css`·`index.html`은 버전 번호 외 변경 없음. **(6) 과도기 안내** — 아직 기능이 없는 3D 조작 버튼(사람·치수·바닥 격자·포인트 벽·시점 ◀▶·PNG)은 마크업은 그대로 두고 표시만 끄고, 막대에 단계 안내 문구를 띄운다(`applyStep1Ui()` — 기능이 붙으면 이 함수만 지우면 된다) | 제안서용 공간 이미지를 실제 공간감 있게 만들기 위해 소프트웨어 렌더러(Canvas 2D·페인터 알고리즘)의 한계를 벗어난다. 깊이 버퍼·조명·텍스처·대량 인스턴싱을 GPU가 처리하므로 SDR 미디어월(518캐비닛) 같은 대형 건도 캐비닛을 전부 그릴 수 있다(기존 렌더러는 2,400셀에서 생략). 단계적으로 바꿔 매 단계 정면 뷰·계산을 그대로 두고 되돌릴 수 있게 한다 | 김현규 | **승인 대기(병합 보류)** |
| DEC-060 | 2026-09-16 | **3D 뷰 최종 미세 보정 4건(오너 지침). 리디자인 아님 — 아래 4개 외 디자인·색·오브젝트·계산은 일절 변경 없음.** **(1) 카메라 높이/하향각** — 최소 하향각 `MIN_PITCH_DEG` 9° → **5.5°**. 하향각은 눈높이(2.35 m)에서 기하로 정해지는데, 방이 깊을수록 각도가 0에 가까워져 최소값이 걸리고, 그 최소값이 클수록 카메라가 눈높이보다 훨씬 높이 뜬다. 깊이 10 m 기준 **3.37 m → 2.49 m**로 내려와 '미니어처를 위에서 보는 느낌'이 사라지고 사람 시점이 된다. 5개 투시 시점 모두 동일 적용, 평면도(정사투영)는 변경 없음. **(2) 포인트 벽 고정** — 기존에는 '카메라에서 보이는 옆벽'에 칠해 시점을 돌리면 포인트 벽이 좌↔우로 옮겨 다녔다(실제 도장은 그럴 수 없음). **공간 좌표 기준 왼쪽 벽**(`ACCENT_WALL_SIDE`)에 고정 — 기본 시점인 '우측 코너'에서 보이는 그 벽. 좌측·좌측 코너처럼 그 벽이 카메라 뒤에 놓이는 시점에서는 컷어웨이로 잘려 안 보이는 것이 맞다. 색·농도(50%)·디자인은 그대로. **(3) LED 중심 framing +10~15%** — 자동 맞춤은 기준점이 전부 들어오는 '가장 안전한' 크기를 골라 카메라가 필요 이상으로 뒤로 빠졌다. **`focusGain`** 이 확대 배율을 1.14→1.10→1.06→1.03→1 순으로 시도해 **LED 네 모서리가 화면 안에 남는 가장 큰 값**을 고른다(잘림 구조적 방지). 배율은 사용자 줌과 곱해지지만 **계산은 줌과 무관** — '맞춤'을 눌러도 같은 framing이 나오고, 확대해도 배율이 되돌아가지 않는다. 평면도는 도면이므로 배율 1 고정. **(4) 데스크톱 3D 캔버스 높이** — `clamp(320px,42vw,560px)` → **`clamp(360px,44vw,640px)`**(모바일 ≤680px 구간은 기존 유지). 1440~1600 px 폭에서 약 634~640 px로 정면 뷰와 비슷한 존재감 | 제안서 이미지가 '다른 3D 프로그램으로 이동한 느낌'이 아니라 '정면 설계를 옆에서 본 느낌'이어야 함. 포인트 벽이 시점마다 옮겨 다니면 같은 공간의 그림으로 읽히지 않음 | 김현규 | Approved |
| DEC-059 | 2026-09-16 | **3D 뷰 비주얼 전면 개선(오너 디자인 지침).** 목표: 정면 뷰와 3D 뷰가 하나의 디자인 언어로 보이게 — '다른 화면으로 바뀐 것'이 아니라 '같은 공간을 옆에서 본 것'. **(1) 카메라: 정사투영(아이소메트릭) → 원근 투시**로 전환. 눈높이 2.35 m·화각 33°·최소 하향각 9°를 모든 시점이 공유(`viewCamera`). 미니어처·CAD 느낌 제거. 시점은 오너 지정 5개(좌측·좌측 코너·정면·우측 코너·우측)+평면도로 정리(기존 9개의 아이소·모서리 중복 제거). **(2) 구도**: 방 전체가 아니라 'LED 벽 + 가구 영역'에 맞춰 프레이밍(`focusAnchors`) → LED가 화면의 시각 초점. **(3) 팔레트**: 지침 색상 적용(뒷벽 #F0F3F8·옆벽 #E7ECF3·바닥 #D4D9E1·포인트 #AFC9BE). 면 밝기 차를 0.93~1.0으로 좁히고 윤곽선 대신 은은한 그라데이션·접지 그림자로만 깊이 표현. **(4) 포인트 벽**: 안쪽 면에만, 농도 50%로 얹어 LED보다 먼저 눈에 띄지 않게. **(5) LED**: 정면 뷰와 같은 짙은 네이비 화면 + 좁은 푸른 심지 + 벽 헤일로, 캐비닛 격자는 흰 선 4.5%, 베젤 1.2px. 원근이라 화면 이미지는 평면 분할로 **원근 보정**. **(6) 치수**: 핵심(가로·세로)은 어두운 알약, 보조(하단 높이)는 흰 알약 — 정면 뷰와 동일 규칙. 오브젝트에 18px로 바짝. **(7) 가구**: 의자를 '등받이+얇은 좌판'으로 단순화, 테이블 받침 슬림화, 무대 높이 450→280, 화분은 원뿔대(`frustumQuads`) 적층으로 둥근 수형. 바닥 격자 기본 ON(정면 뷰와 동일). **계산·배치 로직은 일절 변경 없음** — engine·room-presets의 좌석/배열 계산 그대로 | 제안서에 그대로 쓰는 이미지이므로 두 뷰의 디자인 언어가 갈리면 안 됨. 아이소메트릭은 '모형' 느낌이 강해 AV 설치 시각화에 부적합 | 김현규 | Approved |
| DEC-058 | 2026-09-16 | **3D 뷰 1차 피드백 반영(오너 지적 4건) + 사람 배치.** (1) **벽 꼭짓점 어긋남** — 포인트 벽 색을 벽 윗면·바깥면까지 칠해 흰 벽의 윗면과 만나는 지점에서 색이 끊겼다. 포인트 색을 **'방 안쪽을 향한 면'에만** 적용(`isInnerWallFace`) → 윗면이 흰 띠로 이어져 레퍼런스와 동일. 실제 도장 방식과도 일치. (2) **의자 방향** — 원형·U자·상황실 뒤쪽 테이블 좌석이 등을 돌리고 있었다. 각도를 손으로 적던 방식을 버리고 **`faceTowards(의자, 바라볼 지점)`** 로 전면 교체, 모든 배치가 '맞닿는 테이블 중심'을 넘기도록 통일. 회귀 테스트로 **공간 타입·옵션 100여 조합의 모든 의자**가 맞닿는 테이블(없으면 LED 벽)을 바라보는지 기계 검증. (3) **U자형 좌석 몰림** — 뒤쪽을 먼저 채우던 방식을 **정원 비례 배분**(`distributeSeats`)으로 변경, 뒤·좌·우가 고르게(12석 → 6·3·3). (4) **평면도 좌우 반전** — 위에서 볼 때 카메라 좌우축 기준벡터가 +Z여서 도면이 거울상이었다. -Z로 바꿔 '오른쪽=+X, 위=LED 벽'의 정상 도면. (5) **사람(축척 비교) 추가** — 정면 뷰와 같은 인물 12종·같은 키(mm)를 3D에 빌보드로 세운다. `personSpot`이 LED 옆 빈 곳(가구·무대 회피)을 찾고, 발밑 그림자·키 라벨 표시, 평면도처럼 키가 납작해지는 시점에서는 생략. 3D 막대에 사람 토글·인물 선택 추가(정면 뷰와 값 공유) | 각도를 사람이 적으면 반드시 뒤집힌다 — 규칙을 코드와 테스트로 강제해 재발을 막음(오너 지침: '의자가 맞닿는 테이블을 바라보는지 점검하면서 만들 것'). 포인트 색은 안쪽 면에만 칠하는 것이 실제 인테리어와 렌더 양쪽에서 옳음 | 김현규 | Approved |
| DEC-057 | 2026-09-16 | **작업 브랜치 규칙 확정(오너 지침).** 여러 세션이 동시에 작업하므로 `main` 직접 커밋을 금지하고, 항상 최신 `main`에서 새 브랜치(`claude/<주제>`)를 만들어 작업한다. 한 브랜치 = 한 주제(기능/문서 분리), 푸시·병합 직전 리베이스, 리베이스 후 화면 버전 `v=`를 `main`보다 1 크게 재조정, 같은 줄 충돌은 임의로 덮어쓰지 말고 오너에게 보고. CLAUDE.md '협업 방식' §0으로 기록 | 3D 뷰 작업(PR #3) 중 다른 세션이 v347·v348·v349를 연달아 올려 세 번 리베이스·버전 재조정이 필요했음. 규칙을 명시해 충돌·덮어쓰기를 예방 | 김현규 | Approved |
| DEC-056 | 2026-09-16 | **3D(아이소메트릭) 미리보기 1차 — 범위 변경 승인.** `aidlc-state.md` Out of Scope의 "3D 렌더링"을 **In Scope로 변경**(오너 승인). 03 미리보기에 **정면 뷰 / 3D 뷰** 전환 추가. 3D 뷰는 자유 회전이 아닌 **큐브 시점**(꼭짓점=정아이소메트릭 35.264°, 모서리·면=10°, 평면도 90°)만 제공하고 **정사투영**으로 그려 제품 렌더처럼 평행선이 유지되게 함(오너 제시 레퍼런스 이미지 기준). 카메라 쪽 벽은 잘라내는 **컷어웨이**(돌하우스) 구성 — 바닥 + 반대편 벽 2면 + 포인트(옆) 벽. **공간 타입 프리셋 6종**(회의실·강의실·소/중/대강당·상황실)과 타입별 옵션(테이블 모양·좌석 수·줄/열·통로·무대·교탁·러그·화분)으로 가구를 자동 배치, 방에 안 들어가면 들어가는 만큼만 놓고 안내 문구 표시. 신규 파일 `src/scene3d.js`(카메라·투영·기본 도형)·`src/room-presets.js`(타입·배치)·`src/furniture3d.js`(가구 도형)·`src/render3d.js`(캔버스 렌더)와 테스트 3종 추가. **01 설치 공간에 '공간 깊이 D(m)' 입력 추가**(비우면 공간 타입별 자동). 구성 저장/불러오기에 `spaceD`·`roomType`·`roomOpts` 포함. PNG 3배 해상도 내보내기. 기존 정면 뷰(renderPreview)·engine 계산은 변경 없음 — 3D는 `computeConfig` 결과를 그대로 받아 '좌표로만' 바꾼다(스펙 공식 중복 구현 없음) | 제안서용 공간 이미지를 사내에서 바로 만들기 위함. 외부 라이브러리 없이(CLAUDE.md 규칙 4) 직접 구현. 자유 회전 대신 고정 시점이라 결과가 항상 동일하게 재현되고 그림이 반듯하게 나옴. 삼성 정합성 기준(배열·해상도·전력)은 손대지 않음 | 김현규 | Approved |
| DEC-055 | 2026-09-14 | **비디오월 VMHE/VHHE 상세 + Analog Way Aquilon C MAX 공식자료 반영(signage-specs.md, v302).** 오너 제공 공식조사표 반영. (1) **VMHE(VM55HX-E, 500nit)·VHHE(VH55HX-E, 700nit)**: 패널 IPS·VESA 600×400·On 전력 222/250W·Sleep 0.5W·IR·24/7·DVI In 1 신규 확정(VW_OFFICIAL·IO_DATA). **Max 전력·개별베젤은 공식 미명시 → null 유지(추정 금지)**. **RS232 In/Out·RJ45는 "있음/수량 미상"이나 오너 지시로 기존 값(1) 유지(B안)**. HDMI Out·DP Out은 'Video Out: Yes'만 기재 → null 유지. 출처 URL 2개(한국 카탈로그+홍콩 상세) 보존, 모델코드 KR(…XKR) vs 상세페이지 지역코드(…XXK) 차이 코드 주석. (2) **Aquilon C MAX 신규 추가**(processor-data.js): customizable 6RU, IN 최대 32×4K / Active 24×4K / PGM 16×4K / 믹싱16·분할32, HDCP 1.4·2.2. **정격전력=미상(null), 최대 소비전력 1,500W(전슬롯 SDVoE 최대구성)** 노트에 명확 구분. 카드별 커넥터 수는 장비 고정포트 아님을 명시(출력카드 최대수 null). 앞/뒤 제품이미지(PROC_IMG_IDS) 추가. engine.js·삼성 LED 정합성 불변, npm test 180/180 green | 오너 공식조사표 제공 + "미상=추정금지·출처보존·지금처럼(RS232/RJ45 유지)" | 김현규 | Approved |
| DEC-054 | 2026-09-14 | **단독형 사이니지 소비전력·상세 스펙을 삼성 공식 KR 제품페이지로 갱신(오너 인치별 자료 제공).** 오너 결정: (1) 소비전력 typicalW = 공식 **'소비전력(On Mode)'** 값으로 교체(기존 출처불명 값 대체), maxW는 공식에 없으므로 전 모델 null 유지. (2) **핵심 항목 신규 추가**(스키마 확장): display.panelType, physical.bezelMm, power.sleepW, features.wifi/bluetooth/ir. soc·ratedUsage 기존 필드 채움. (3) 진행: 인치별 수집. **반영 완료: QHC 6종(v285)** + **QMC 7종(QM32~85C, v286)** — 전 모델 패널 VA·Tizen 7.0·WiFi/BT/IR·Sleep 0.5W·24/7, On Mode 전력, 베젤(11.5/13.4/13.9mm). QM50C·QM85C 응답 10ms 정정. QM32C=FHD 1920×1080·400nit·DP In 0 확인. 04 산출 스펙에 On/Sleep·패널·베젤·SoC/OS·사용시간·무선(조건부) 표시. **미제공/미확인**: QM98C(공식표 미제공→기존값 유지), QH115FX 패널·베젤·Sleep·SoC·무선(축약표에 없음), QM43C·QM50C 스피커, 비디오월 6종 상세(수집 범위 밖), 전 모델 maxW. engine.js 무관·삼성 LED 정합성 불변, npm test 174/174 | 오너 공식 자료 제공 + "On Mode로 교체·핵심만 추가·인치별 반영" 선택 | 김현규 | Approved |
| DEC-053 | 2026-09-14 | **03 미리보기 사이니지 표시·명칭·공간 초과/확장 UX(v277~280, 표시 전용).** (1) v277: 사이니지(단독형·비디오월) 패널을 LED 픽셀 매트릭스가 아니라 **예전 LED '그라데이션 블록'**(`.rs3Frame.svPanel .rs3Grid>i`)으로 표시 — LCD 화면 느낌. (2) v278~279: 표시 문구 일반화 — 03 토글 'LED 격자'→'**패널 격자**', 01 입력 'LED 하단 높이'→'**디스플레이 하단 높이**'(사이니지 공용). (3) v280: **배열이 선언 공간을 넘지 않게 자동 제한**(LED 배열 직접 지정·사이니지 비디오월 공통) + 초과 시 **인라인 안내 + [공간 넓혀 확장] 버튼**(오너 선택) → 클릭 시 **공간확정 확인창(confirm)** 후 01 공간을 필요한 크기로 자동 설정. **규칙: 좌우 여백 각 500mm 고정(500~600mm 범위)·화면 하단 높이 유지(위로만 확장)**. `computeSvFit`/`ledManualFit` 공용 헬퍼, opts()·clampManualArray는 요청값 유지·계산은 제한값. **engine.js(공식·삼성 정합성) 무변경**, 168/168 green. UI 실동작은 라이브 확인 필요(헤드리스 차단) | 오너 요청(그라데이션 블록·명칭변경·"공간 초과 방지 + 팝업 확장, 확장 시 공간확정 확인 후 자동, 좌우여백 500~600mm 고정, 화면하단 유지") + 안내방식 '인라인+버튼' 오너 선택 | 김현규 | Approved |
| DEC-052 | 2026-09-14 | **사이니지 20종 verification 최종검증 완료 → 'verified' 승격(오너 지시 1·2 진행).** ①단독형 QMC/QHC 14: 외형·무게·VESA·전력·해상도를 독립 출처와 교차확인(QM55C 15.7kg / QM98C 48.1mm·56.3kg / QH115FX 2565.2×1467.6×34.1mm·83.7kg·VESA1000×600 **정확 일치**), 물리 스펙은 지역 공통(동일 패널)이라 KR SKU(BXKR)에 유효 → status 'official_catalog'→'verified'. ②비디오월 6: 오너 승인 자료 + 교차확인(VMC-R 0.88mm=**Bezel-to-Bezel**, 개별 0.44mm·500nit·IP5X·깊이<70mm) → status 'user_approved_source'→'verified', **bezelDefinition 'catalog_value'→'bezel_to_bezel'**(오너 미확인 항목 해소). 미확인 값(powerMax·SoC·io 등)은 null 유지. 삼성 공식 사이트 직접열람은 환경 차단이라 독립 출처 다수 교차확인으로 대체(방법 명시). 160/160 green | 오너 "1,2 모두 진행해" | 김현규 | Approved |
| DEC-051 | 2026-09-14 | **삼성 LCD 사이니지 라이브러리·배치·03 3D 표시 배포(v272) + 사이니지 verification 잠정 유지.** signage-data.js(20종)·02 선택 팝업·03 3D 미리보기(단독 실물풍/비디오월 이미지) 라이브 반영. 단독형 physical/power/VESA는 데이터시트 수집값이나 **한국 SKU(BXKR) 교차검증 전 잠정** — verification 최종확정 승격 안 함(오너 지침). 다음 단계: ①QMC/QHC 14 KR SKU 검증 ②비디오월 6 상세검증. LED/프로세서 로직 무수정 | 오너 "배포해줘" + "verification 최종확정 승격 금지" | 김현규 | Approved |
| DEC-050 | 2026-09-14 | **광 미사용(비-CS4B) 컨트롤러의 IG(신호 입력 그룹) 단위 = FHD 1920×1080(이사 지침).** IFR/IEA(SNOWJMU)·IFR-M(CS4FPGS) 등 비-CS4B는 08 데이터 흐름 신호 그룹을 1920×1080으로 분할(CS4B 광지빅=1920×2160, 그 외 기존 S-Box 4K는 비-CS4B에서 FHD로 대체). engine `fhdLayout`+`IG_FHD_W/H` 추가, computeConfig `igFhd` 노출(비-CS4B·비통합만). renderDataFlow에 IG(FHD) 분기·제목/캡션. S-Box 대수 산식(3840×2160)·정합성 불변, 도면 신호 그룹 단위만 변경. 회귀테스트 추가 | 오너 지침("IFR-M·IEA 등 광 미사용 시 Ig는 1920×1080 단위") | 김현규 | Approved |
| DEC-049 | 2026-09-14 | **GBIC 그룹 라벨에 S-Box 출력 포트 표기(오너 지침).** 라벨을 `SBOX#<s>-<p> · GBIC #<n> · 열범위`로 변경(s=S-Box 번호, p=포트). CS4B 1 S-Box=출력 4포트=GBIC 2개, **Primary 1·2 + Redundant 3·4**(오너 확인). 매핑: GBIC region의 px0/py0 → S-Box(3840×2160) 격자에서 sboxNo, S-Box 내 위치→port(=floor((px0-sboxCol*3840)/1920)+1). 캡션에 포트 규칙 안내 추가. 좁은 영역은 라벨 길이 순 자동 축약. 표시 전용, 144/144 green | 오너 지침("SBOX#1-1 GBIC#1·1~5열 / SBOX#1-2 GBIC#2·6~10열; 1 S-Box 출력 1·2포트 Primary, 3·4 Redundant") | 김현규 | Approved |
| DEC-048 | 2026-09-14 | **CS4B 데이터 흐름 도면을 GBIC(1920×2160) 단위로 분할(오너 정정) + 이미지 팝업 풀스크린 최상단.** (1) CS4B 계열은 광지빅을 1920×2160 px마다 사용 → 도면 신호 그룹을 S-Box(4K=3840×2160)가 아니라 **GBIC 격자(1920×2160)** 로 분할. engine에 `gbicLayout` 추가(igLayout 타일러 공용화 `tileWall`), computeConfig에 `igGbic` 노출. renderDataFlow는 CS4B면 igGbic 사용·라벨 'GBIC #N'·제목/범례/캡션 GBIC 표기. 검증: MM015F 10×10(3840×2160)=GBIC 2영역(1~5열/6~10열, 삼성 P23/25 일치), S-Box로는 1영역. MP012F 6×3.4=GBIC 6영역(3×2)·S-Box 4. 회귀테스트 2건 추가(144/144). (2) 이미지 넣기 팝업 z-index 1000→2000으로 풀스크린(#pvZoom 1000) 위에 표시. 계산/정합성 불변(GBIC SET 수량 산식 기존과 동일) | 오너 정정("CS4B는 1920×2160 단위로 지빅 사용, MMF P1.5 10×10 첨부 이미지") + 팝업 풀스크린 최상단 요청 | 김현규 | Approved |
| DEC-047 | 2026-09-14 | **이미지 넣기 방식 선택(가로/세로 고정) 팝업 + 검은 테두리 실제 적용 + S-Box 라벨 축소.** (1) '이미지 넣기' 클릭 시 '가로 고정(가로 꽉·상하 이동)'/'세로 고정(세로 꽉·좌우 이동)' 선택 팝업 → 파일 선택. renderPreview에 세로 고정 분기(pvImgMode='height', pvImgPanX) + 드래그 좌우 이동 추가. (2) **DEC-046 재수정**: 검은 테두리를 styles.css에 넣었으나 `.rs3Led`는 renderPreview의 인라인 box-shadow(ledGlow)가 우선이라 무효였음 → ledGlow 테두리색 `#2f7ff6`→`#050608`로 변경(실제 적용). 죽은 CSS 되돌림. (3) 08 S-Box 라벨 글자 축소(cs*0.42→0.32, max13→10). 표시 전용, 142/142 green | 오너 요청(이미지 가로/세로 고정 선택 · 라벨 축소) + v266 검은 테두리 미적용 자체수정 | 김현규 | Approved |
| DEC-046 | 2026-09-14 | **03 미리보기 LED 벽 검은 테두리(베젤) + 파란 네온 글로우 유지(오너 요청).** `.rs3Led` box-shadow에 (1) 바로 붙는 검은 링 `0 0 0 2.5px #050608`, (2) 그 뒤로 퍼지는 파란 네온 2겹(rgba(47,127,246,.5)/.26 blur) 추가. 기존 `.rs3Glow`(면 위 파란 숨쉬는 워시)는 그대로 유지. 표시 전용·계산 무관, 142/142 green | 오너 요청("LED 테두리 검은색, 뒤로 파란 네온 유지"). 디자인성 파일 최소 수정(1개 규칙) | 김현규 | Approved |
| DEC-045 | 2026-09-14 | **CS4B 광 I/G 이중화 도면 트리거 조건 수정(버그픽스).** DEC-044에서 도면 표시 조건을 `redundancy`(SBOX 이중화)로 잘못 연결 → 오너가 실제로 켜는 **'Gbic 포워드/백워드(광 이중화)' 체크박스(gbicFB)** 로는 도면이 안 나왔음. 조건을 `r.gbic!=null && (r.gbicFB || r.redundancy)`로 변경, engine.js computeConfig 반환에 `gbicFB` 필드 추가(계산값 불변, 노출만). 흐름 헤더 GBIC 노드에 '+광 이중화' 표기. **추가: 각 S-Box 신호 그룹(빨간 테두리) 하단에 작은 라벨(`S-Box #N · X~Y열`, 좁은 1열 영역은 `#N`)** 표시(오너 제안). 142/142 green | 오너 지적("CS4B 이중화 구현안됨") + 오너 제안(그룹별 S-Box 번호 라벨). '광 I/G 이중화'=광지빅 포워드/백워드(gbicFB)이며 SBOX 이중화와 별개. 계산/정합성 불변 | 김현규 | Approved |
| DEC-044 | 2026-09-14 | **CS4B 광 I/G 이중화 시각화(삼성 MM015F P23/25 Data Flow Diagram 준용).** 08 데이터 흐름 도면에서 이중화(redundancy) + CS4B(gbic≠null)일 때 각 S-Box 신호 그룹을 **Primary(파란 ●, 체인 시작)·Redundant(빨간 ▬, 체인 반대쪽 끝)** 두 끝에서 급전하는 것으로 표시(한쪽 광선로 장애 시 반대쪽 계속 동작). 1캐비닛 영역은 두 표식을 살짝 어긋나게 배치해 둘 다 보이게 함. 범례에 Redundant Data Link 항목·제목에 '광 I/G 이중화' 추가. 계산 로직·정합성 기준 불변(표시 전용). 142/142 green. 별도로 노바스타 등 카드형 프로세서 상세 팝업 표시 수정(v263) | 오너 요청("P23/25 참고 CS4B 광 I/G 이중화 이미지"). engine.js 계산 무변경, app.js dataFlowSVG 표시만 확장(중복 계산 금지 규칙 준수) | 김현규 | Approved |
| DEC-043 | 2026-09-13 | MPF(MP008F/012F/016F) 기본 컨트롤러를 SBB-SNOWAAE → **SBB-CS4BPGS**로 변경(DEC-015 대체). MMF·MPF 모두 CS4B 계열 → 광지빅(GBIC) 자동 산출. 08 데이터 흐름 '구성도'에 S-Box→광지빅(GBIC)→LED 경로 노드 표시 | 이사 지침(MMF·MPF 모두 CS4B·광지빅 사용) + 삼성 공식 검증(MP012F Fit-to-wall 시 S-Box=SBB-CS4BPGS). S-Box 수량 산식(박스당 4K)·전력·해상도 등 계산 불변, 컨트롤러 표시명과 GBIC 산출만 변경. 정합성 회귀 테스트(GBIC/견적)를 새 기준으로 갱신, 142/142 green | 김현규 | Approved |
| DEC-001 | 2026-07-23 | vanilla JS 단일 HTML 채택, engine/data/UI 분리 | 배포 단순, 의존성 0, 사내 오프라인 실행 용이 | 김현규 | Approved |
| DEC-002 | 2026-07-23 | 산출 항목·데이터 스키마를 삼성 실측 기준으로 정렬(Pitch, W×H×D, weight, max/typ power, resolution, brightness, OVD, BOM) | 사내 견적 정합성 확보 | 김현규 | Proposed |
| DEC-003 | 2026-07-23 | 평균전력 계수 0.35 → 0.53 정정 | 삼성 MP012F 실측(3234/6132=0.527) 반영 | 김현규 | Proposed |
| DEC-004 | 2026-07-23 | 방향 A 채택 — 본 프로젝트 유지 + DISPLAY FIT 우수 요소(S-Box 규칙·dataStatus 3단계·검증 전력) 흡수. feet 미도입 | 가벼운 배포 이점 유지하며 산출 정확도 보강 | 김현규 | Approved |
| DEC-005 | 2026-07-23 | S-Box 수량 = `ceil(전체픽셀/컨트롤러용량)`, 이중화 ×2 | 삼성 검증(42캐비닛→2대) 일치 | 김현규 | Superseded by DEC-006 |
| DEC-006 | 2026-07-24 | S-Box 수량 = 영역 타일 `ceil(resW/3840)×ceil(resH/2160)`, 이중화 ×2. SBB-CS4B(=CS4BPGS)·SNOWAAE 공통 4K. IFR/IEA에도 용량 반영. Outdoor(IB) 단종 삭제. 슈퍼와이드 시 16:9 최대해상도 산출 | S-Box 데이터시트 스터디(CS4B/SNOWAAE 매뉴얼)로 박스당 최대 4K 확인, 영역 타일이 실제 신호 매핑에 부합하고 삼성 42→2 검증 유지. SNOWAAE 8K는 4K로 보수 운용 | 김현규 | Approved |
| DEC-007 | 2026-07-24 | 모델 라이브러리 정리: IW 시리즈(IW008C/012C/016C) 삭제(단종·MPF로 대체); 픽셀피치 2.5mm 초과 및 P2.0(IF020R) 모델 기본 숨김(데이터 보존, 필터 제외); MP008F 부품코드 LH008MPFAAA 반영(삼성 DE 데이터시트); 캐비닛 치수 표시를 소수점 1자리(예: 806.4×453.6)로 정밀화 | 세일즈 대상 피치 라인 한정 + 데이터시트 부품코드 확보 + 453.6mm를 454로 반올림 표기하던 오해 해소(데이터는 삼성 검증값 453.6 유지) | 김현규 | Approved |
| DEC-008 | 2026-07-24 | 밝기 표시를 peak → "최대"(운영 최대)로 변경. 산출 스펙·비교표 모두 `brightnessMax = brightnessReduced ?? brightnessPeak` 사용 | 삼성 세일즈 자료가 "최대 밝기" 기준(예: MPF 1000 / IFR 800 / IEA 500 / MMF 600 nit)을 사용 — 실제 견적/운영 값에 부합 | 김현규 | Approved |
| DEC-009 | 2026-07-24 | 기본 노출 피치 범위를 P0.8~P1.8로 한정(기존 ≤2.5). 범위 밖(IF020R P2.0, IF025R/IE025A P2.5, IF040R P4.0)은 데이터 보존한 채 기본 숨김 | 삼성 판매 정책상 앞으로 P0.8~P1.8만 판매. 안 팔리는 피치는 기본 목록에서 제외(데이터는 유지, 필터만 조정) | 김현규 | Approved |
| DEC-010 | 2026-07-24 | MMF 시리즈에 MM009F(P0.9375, 640×360)·MM012F(P1.25, 480×270) 추가. 밝기 최대 600 nit, 최대전력 85.8W·92.7W(=삼성 시트 W/㎡ × 캐비닛 0.2025㎡), 부품코드 LH009MMFRGS·LH012MMFRGS. weight/typical은 세일즈 시트에 없어 null(데이터시트 필요) | 삼성 MMF 세일즈 자료(slide 13)로 피치·해상도·밝기·최대전력(W/㎡) 확인. MM015F(467→94.6W) 검증값이 동일 산식으로 재현되어 산식 신뢰 | 김현규 | Approved |
| DEC-011 | 2026-07-24 | 모델 라이브러리 기본 표시 순서를 IFR→IEA→MMF→MPF로 변경(SALES_LINES 순서 + stable sort, 라인 내부·커스텀·JSON 순서 보존) | 오너 설계 우선순위 반영 | 김현규 | Approved |
| DEC-012 | 2026-07-24 | IFR/IEA 기본 컨트롤러(S-Box)를 SBB-SNOWJMU로 변경(필요시 CS4B). 산출 스펙의 S-Box 항목이 모델별 실제 컨트롤러명을 표시하도록 개선(하드코딩 라벨 제거). MPF/MMF는 SBB-CS4BPGS 유지 | IFR/IEA는 SNOWJMU로 주 설계, CS4B는 예비. S-Box **수량 산식(박스당 4K, maxInputW/H)** 은 SNOWJMU 용량 데이터 미확보로 변경하지 않음(수량 결과 불변, 삼성 정합성 유지) | 김현규 | Approved |
| DEC-016 | 2026-07-24 | 전체 서체를 Pretendard로 변경(본문·숫자 통일, 애플 SF 느낌 + 완전한 한글). 가변 woff2를 저장소에 동봉(self-host, `src/fonts/PretendardVariable.woff2`), `--sans`·`--mono` 모두 Pretendard 우선 + tabular-nums | 애플/삼성 정품 폰트는 웹 임베드 라이선스 제약 → 오픈 라이선스(OFL) Pretendard 채택. CDN 대신 파일 동봉으로 빌드리스·오프라인 유지(외부 런타임 의존성 없음). 계산 로직 무관(디자인만) | 김현규 | Approved |
| DEC-015 | 2026-07-24 | MPF(MP008F/012F/016F) 기본 컨트롤러를 SBB-SNOWAAE로 변경(SNOWAAE 또는 CS4B 사용 가능, 기본 SNOWAAE). MMF는 SBB-CS4BPGS 유지 | 오너 설계 기준. SNOWAAE도 4K(3840×2160) 상한이라 maxInputW/H·S-Box 수량 산식 불변 → MP012F 42→2 정합성 유지(수량 결과 동일, 컨트롤러 표시명만 변경) | 김현규 | Approved |
| DEC-014 | 2026-07-24 | 광 지빅(GBIC) 산출 추가: '01 설치공간'에 'CS4B(광전송)로 설계' 토글. 켜면 컨트롤러를 SBB-CS4B로 보고 광 지빅 SET = ⌈resW/1920⌉×⌈resH/2160⌉(1 SET=SBOX 1+LED 1, 이중화 ×2)을 S-Box 옆에 표기. 기본 OFF | CS4B(광전송) 구성에서 1920×2160마다 광모듈(Exatek EXA-40G-QSFP-LR4 등) 1 SET 필요(오너 지침). 삼성 CS4BPGS export엔 지빅 없음 → 기본 OFF로 정합성 유지, 필요시(CS4B)만 산출. S-Box 수량 산식은 불변 | 김현규 | Approved |
| DEC-013 | 2026-07-24 | MM009F·MM012F를 삼성 configurator export로 검증·확정(dataStatus derived→verified). 무게 5.1kg 공통, 최대/평균전력(W/캐비닛) 009F 94.6/37·012F 92.8/41.5, OVD 009F 3.2m·012F 4.3m 반영. **009F 최대전력 85.8W→94.6W 정정**(이전 세일즈 시트 423.47 W/m² 값 오류, export 6811.2W/72=94.6 기준) | 삼성 공식 export가 진실 소스. 검증: 009F 12×6(367.2kg·6811.2W·2664W·2 S-Box)·012F 8×8(326.4kg·5939.2W·2656W·1 S-Box) 전 항목 일치. 참고: 두 export 모두 광 지빅(GBIC) 부품 미표기 → CS4BPGS는 별도 지빅 불필요 확인 | 김현규 | Approved |

| DEC-017 | 2026-09-11 | (로드맵/Proposed) 소프트웨어 범위 확장 방향 채택 — 단순 치수 계산기 → **LED 연출 시뮬레이션 + 프로세서 자동 추천 + 견적 연동**. (a) LED 캔버스 위 레이어 드래그 배치·리사이즈(ICS/Analog Way "세미 버전"), 배경/격자벽 샘플 이미지로 룩앤필, (b) 공간·해상도별 레이어/분할 한계 분석 → 프로세서(Aquilon C ~ NovaStar) 자동 리커맨드, (c) 연출 확정 시 추천 프로세서 + 장비금액 포함 최종 견적. **시인성 규칙(오너):** 분할 창은 130인치 4분할=65인치까지 실효성 있음, 그보다 작으면 시인성 저하 → 분할 창 대각 하한 ≈ **65인치**를 추천 기준으로 사용. 프로세서 제품별 최대 레이어·분할·입력해상도 데이터는 오너가 정리해 제공 예정(그 전엔 규칙/엔진 미구현) | 현장 구축 단계 설계오류·시공 리스크 사전 차감, 초기 LED 사이즈 확정 후 AV/비디오 설계 검증 속도 단축 | 김현규 | Proposed |
| DEC-044 | 2026-09-12 | **치수 라벨 축소·행번호 안쪽·여백 위치(v197, 이사 요청).** (1) 치수 라벨(rs3Dlbl key/sub/count/person·eyeLbl) 크기 0.7배(캐비닛 번호 rs3Num 14px는 유지). (2) 세로(행) 캐비닛 번호를 LED 왼쪽 바깥→**맨 왼쪽 칸 안쪽**(밝은 글자, 열 번호와 코너 겹침 방지 오프셋). (3) 좌우 여백 치수를 여백 중앙→**캐비닛 바로 옆(가장자리 근처, 세로 중앙)**. 계산 불변, 115/115, 헤드리스 확인 | 라벨 과대·행번호 위치·여백 표기 정리 | 김현규 | Approved |
| DEC-043 | 2026-09-12 | **치수선·번호 정렬 틀어짐 버그 수정(v196, 이사 피드백).** v195에서 오버레이(치수·번호)를 스케일 캔버스 '안'에 둔 채 proj에도 ×fit을 넣어 **fit이 두 번 적용**→좌표가 왼쪽으로 밀리고 좁아져 3D LED와 어긋남. 수정: v2 스펙 **B안** 채택 — proj는 캔버스 좌표만 반환(×fit 제거, 오버레이는 캔버스 안에서 3D와 같은 좌표계로 항상 정렬), 라벨 글자만 `calc(base/var(--fit))`로 역보정해 화면 크기 유지(치수 12·번호 14·눈높이 9px). frame에 `--fit` 주입. 헤드리스 측정: 열번호 중심이 LED 범위 안에 정렬, 글자 화면 14px. 계산 불변, 115/115 | fit 이중적용 제거, 오버레이 정렬 보장(타이밍 무관) | 김현규 | Approved |
| DEC-042 | 2026-09-12 | **미리보기 원근감 v2 보완 패치 적용(v195, Claude Design 코드리뷰 원근감-스펙 v2).** 적용 9/11: (1-1) 치수·눈높이선·모서리 오버레이 레이어를 스케일 캔버스 밖으로 올리고 proj()가 fit 곱한 실화면 좌표 반환 → 라벨이 캔버스 축소에 안 줄어듦(모든 화면 14/12px 유지). (1-2) 캐비닛 번호를 3D 면 자식→2D 오버레이(열=캐비닛 위 밝은 글자, 행=벽/LED좌측, 간격<22px면 솎아냄). (1-3) 사람 위치 벽에서 0.6m→3.5m·측벽 1.5m(스케일 기준물). (1-4) `.rs3Face` backface-visibility 제거(옆벽 소실 방지). (2-2) camMM=max(depth+2m, sW*0.9, 6000). (2-3) 깊이단서 px고정→mm환산(AO 1.5m·글로우 4m·LED글로우 0.6/0.1m·grid gap=cell*2%). (2-4) 앞쪽 폭 치수선 clamp(캔버스밖·사람 겹침 방지). (3-2) print·reduced-motion 글로우 애니 정지. (3-3) 미사용 옛 rs*·pvScene CSS ~90줄 삭제. **보류/보고:** (2-1) 초점/카메라 분리는 투영식 전면 재작성이라 별도. (3-1) 사람↔눈높이선 분리는 이사 이전 지침(묶기, DEC-034/037)과 상충 → 현행 유지, 이사 확인 대기. 계산·정합성 불변, 115/115, 헤드리스 5케이스(여백·30m대형 솎아냄·좁은화면·전체채움) 검증 | 라벨 판독성·대형 배열·인쇄 일관성 등 리뷰 지적 반영 | 김현규 | Approved |
| DEC-041 | 2026-09-12 | **키 표기·여백 치수 위치 이동(v194, 이사 요청).** (1) '키 170cm' 라벨을 하단 왼쪽 구석 → **사람 기호 옆(발밑, proj)** 으로 이동. (2) 좌우 여백 치수를 LED 하단 → **캐비닛 좌우 옆(세로 중앙, v=Ly+Lh/2)** 으로 이동. 계산 불변, 115/115, 헤드리스 확인 | 라벨 가독·연관 위치 개선 | 김현규 | Approved |
| DEC-040 | 2026-09-12 | **좌측벽 그라데이션을 우측과 일치(v193, 이사 요청).** 좌·우 옆벽 그라데이션 방향이 반대(wallL `to right`, wallR `to left`)라 좌측이 밋밋해 보이던 문제 → wallL을 wallR과 동일(`to left,#ccd1da,#eceef3`)로 맞춰 양쪽 벽이 대칭 입체감. 계산 불변, 115/115 | 좌우 벽 음영 대칭 | 김현규 | Approved |
| DEC-039 | 2026-09-12 | **캐비닛 숫자 확대 + 방 모서리 오버레이 선(v192, 이사 피드백).** (1) 캐비닛 안 열/행 숫자 폰트 9.5px→14px(약 1.5배). (2) **좌측벽↔천장 경계가 계속 안 잡히던 문제** — 3D 면 테두리(border)는 렌더 불안정 → 제거하고 **방 8개 구조 모서리(천장·바닥↔좌우벽 깊이 4모서리 + 뒷벽 둘레 4변)를 2D 오버레이 선(proj)** 으로 확실히 그림(rs3EdgeLayer/rs3Edge). 면 명도 그라디언트는 유지. 계산 불변, 115/115, 헤드리스(모서리 8·숫자 14px) 확인 | 3D 면 테두리 불안정 → 오버레이로 방 enclosure 모서리 확정 | 김현규 | Approved |
| DEC-038 | 2026-09-12 | **벽면 크기 입력 m 단위화 + 바닥 가로 격자 강화(v191, 이사 요청).** (1) **벽면(#spaceW/#spaceH) 입력을 m 단위로**(라벨·기본값 8/3.4·step 0.1·min 0.5). **내부 계산·저장·공유·테스트는 모두 mm 그대로 유지** — 읽기 지점 32곳을 `spaceWmm()/spaceHmm()`(×1000) 헬퍼로 치환, applyConfig는 화면에 c.spaceW/1000로 되씀, 요약 표기 m로. config.js·engine·정합성 불변. LED 설치 크기·하단 높이는 mm 유지(요청 범위=벽면). (2) **바닥 가로 격자(깊이 방향 좌우 선) 강화** — to bottom 라인 rgba .34/1.5px, 세로(수렴)선은 .16로 은은하게. 계산 불변, 115/115, 헤드리스(12×4.5 m→내부 12000/4500mm 정확·격자) 확인 | 실무 입력 편의(m), 바닥 원근 가독성 | 김현규 | Approved |
| DEC-037 | 2026-09-12 | **눈높이선 버튼 상태 일치(v190, 이사 피드백).** 사람 끄면 눈높이선은 사라지는데 눈높이선 버튼은 켜진 채라 헷갈리던 문제 → `syncPvToggles()`로 버튼 표시를 실제 상태와 일치: 사람 꺼지면 눈높이선 버튼도 꺼짐 표시 + 흐리게(pvTogDim) + 클릭 무시. 사람 다시 켜면 복귀. 계산 불변, 115/115, 헤드리스 상태전이 검증 | 종속 토글(눈높이선←사람) 버튼 상태 정합 | 김현규 | Approved |
| DEC-036 | 2026-09-12 | **03 3D 미리보기 버그·경계 수정(v189, 이사 피드백).** (A) **눈높이선이 바닥에 그려지던 버그** — 회전된 좌측벽의 자식으로 넣어 3D 동일평면 렌더가 깨져 바닥에 조각만 걸치던 문제 → 눈높이선을 **2D 오버레이(proj 투영)로 재구현**(별도 rs3EyeLayer, 뒷벽~앞쪽 두 점을 이어 좌측벽 따라 수렴). 이제 좌측벽 위 1.6m/1.2m에 정확히 표시(사람/눈높이 토글로 제어 유지). (B) **천장↔옆벽·바닥↔옆벽 모서리 경계 없음** → 천장·바닥 면에 border-left/right 추가로 방 모서리 크리스프. 계산 불변, 115/115, 헤드리스 확인 | 눈높이선 위치 버그 해결 + 방 enclosure 모서리 명확화 | 김현규 | Approved |
| DEC-035 | 2026-09-12 | **03 3D 미리보기 방 위쪽(천장·옆벽) 정의 강화(v188, 이사 피드백 "좌측벽·천장 부족").** 천장·옆벽 면색이 프레임 배경과 거의 같아 방 위쪽이 안 읽히던 문제 → (1) 면 명도 대비 강화(천장·옆벽을 배경보다 뚜렷이 어둡게, 뒤로 갈수록 어둡게), (2) 각 면의 **뒷벽쪽 모서리에 경계선**(ceil/floor border-top, wallL/wallR border-left) 추가로 방 모서리가 크리스프하게, (3) 정면(뒷)벽 inset 그림자로 방 안에 앉힘, box-sizing:border-box. 계산 불변, 115/115, 헤드리스(전체채움·여백 배치) 확인 | 방 enclosure가 배경과 구분되어 원근감·좌측벽·천장이 또렷 | 김현규 | Approved |
| DEC-034 | 2026-09-12 | **03 3D 미리보기 이사 피드백 반영(v187).** (1) 사람 도형을 예전 실루엣(머리 원+어깨 넓은 몸통+다리)으로 복원(rs3Person .h/.b/.l, 폭 300mm). (2) '키 170cm' 라벨을 얼굴 옆 → **하단 왼쪽 구석 고정**. (3) **원근감 강화**: 스케일 기준을 '뒷벽 채움'(÷f0, 벽이 화면을 덮어 방 깊이가 사라짐)에서 **'방 앞면(카메라쪽 개구부) 채움'(÷fFront)** 으로 변경 → 방이 화면을 꽉 채우고 뒷벽(LED)은 안쪽에 ~58%로 물러나 바닥·천장·옆벽이 소실점으로 강하게 수렴(Claude Design 시안과 일치). 방 깊이 0.85×폭(cap 12000). (4) **면별 명도차·바닥 그리드 강화**(천장 최명→바닥 최암, grid rgba .22)로 방(모서리)이 또렷이 읽힘. (5) **사람 토글 끄면 눈높이선도 함께 숨김**. 계산 불변, 115/115, 헤드리스 확인(여백 배치=시안 원근감 일치·전체채움·사람off) | 원근감이 Claude Design만큼 안 산다는 피드백 → 벽이 아닌 방 앞면 기준으로 맞춰 깊이 확보 | 김현규 | Approved |
| DEC-033 | 2026-09-12 | **03 미리보기 CSS 3D 1점 투시 원근 적용(이사 디자인 핸드오프, 원근감-스펙.md).** 기존 2D %좌표 방식(rsFrame/rsWall/…) → CSS 3D 방(정면벽·바닥·천장·좌우벽 5면, 같은 소실점 수렴)으로 renderPreview 전면 재작성. **치수 값·계산(computeConfig)은 그대로 사용**하고 위치만 투영식 proj()로 2D 오버레이 배치(글자 안 찌그러짐). 스펙의 고정 좌표(9×3/FHD 예시) 대신 **실제 계산값에서 동적 산출**(S=화면맞춤 px/mm, 정면벽 투영배율 0.5 보정) → 모든 벽 크기·배열·LED크기·하단높이에서 동작. 깊이 단서: 바닥 1m 그리드·접지음영·LED 글로우·면별 명도차. 요소: LED월(정면벽 자식, 캐비닛 그리드+열/행 번호 안쪽 유지)·FHD/UHD 신호 오버레이·눈높이선(좌측벽)·사람 빌보드(씬 좌표+translateZ). **표시 토글 4개(사람/눈높이선/바닥 그리드/치수)** 신설 — 치수 끄면 발표용 클린 3D 뷰. rs3* CSS 신설(항상 라이트=물리 다이어그램), 구 rs* 블록은 미사용(주석 표기, 추후 정리). id/data-* 보존. v186. 계산·정합성 불변, 테스트 115/115, 헤드리스 다중 크기·토글·FHD 검증(에러 없음) | 원근감 강화로 공간 이해도 향상(이사 승인). 고정 예시가 아닌 동적화로 실사용 모든 케이스 대응. 치수 계산은 절대 불변 | 김현규 | Approved |
| DEC-032 | 2026-09-12 | **05 프로세서 추천 개선: 작은 모델 우선 + 제조사별 접이식 + 섹션 접힘(이사 지침).** (1) **정렬**: 동급 내 정렬 기준을 '여유(headroom) 큰 순'→'**여유 적은(적정 크기) 순**'으로 변경(rankProcessors). 소형 작업(예: 5760×1080, 필요 4K 출력 2)에서 대용량(H20 등)이 위로 오던 문제 해결 — 이제 적정 모델(Alta·X100 2U/4U·H2 등) 먼저, 대용량은 후순위. (2) **제조사별 그룹화**: 추천 결과를 Analog Way/NovaStar/Colorlight로 묶어 접이식(details)으로 표시(vpGroupsHTML), 최상위 모델을 가진 제조사만 기본 펼침·나머지는 눌러서 펼침. 접힌 상태에서도 제조사·최상위 등급·대표 모델·개수 표시. (3) **05 섹션 자체 기본 접힘**: #vpCard를 details로 전환(내부 id/data-* 전부 보존). v185. 계산·판정 로직·데이터 불변(정렬만 변경), 테스트 115/115, 헤드리스 확인(섹션 접힘·그룹 3개·에러 없음) | 소형 작업 과사양 억제, 긴 목록 정리. 숨김이 아닌 후순위+접기로 정보는 유지 | 김현규 | Approved |
| DEC-031 | 2026-09-12 | **미리보기 캐비닛 번호를 캐비닛 안쪽에 표기(이사 요청).** 기존에는 열 번호를 패널 위 남는 틈(topGap, 최대 5%)에 배치 → 공간이 클수록(위 여백이 좁을수록) 번호가 찌그러져 안 보임. 수정: 열 번호를 **맨 윗줄 캐비닛 안쪽**(top row band), 행 번호를 **맨 왼쪽 칸 안쪽**(left column)에 겹쳐 표시. app.js renderPreview의 rsColN/rsRowN 좌표를 패널 내부(행높이/열너비 기준)로 변경, `.inGrid` 스타일 추가(다크 캐비닛 위 가독성: 밝은 글자+그림자). 미사용 rowNL 제거. v184. 계산·id/data-* 불변, 테스트 115/115 유지, 헤드리스 렌더 확인(12×3 번호 안쪽·에러 없음) | 큰 공간에서 번호가 안 보이는 문제 해결, 예전 방식(안쪽 표기) 복원 | 김현규 | Approved |
| DEC-030 | 2026-09-12 | **프로세서 코드 3분할(이사 승인).** engine.js에 몰려 있던 프로세서 로직을 역할별 3파일로 분리(기능·화면·계산결과 무변경, 순수 구조 개선): 데이터 `processors.js`→`src/processor-data.js`(rename), 용량·한계 계산 `src/processor-limits.js` 신설(processorRequirements·regionTiles·inputsCapacity·outputs4kCapacity·outputCardUsage2kEq·validateOutputCardLayers·TILE/LAYER 상수·INPUT_2K_PER_CARD), 판정·등급·정렬 `src/processor-validator.js` 신설(validateProcessor·rankProcessors·validateBuild·gradeLabel·GRADE_ORDER·AQUILON_HIDE). engine.js는 LED 코어 계산 전용(프로세서 코드 제거). app.js·tests·index.html import·캐시버전 v182→v183 갱신, 내부 import(validator→limits)도 ?v=183. SOURCE-OF-TRUTH §6·README 매핑 갱신. **테스트 115/115 green, 헤드리스 로드 무오류 확인** | 유지보수성(사양 추가 시 위치 명확). 무동작-변경 리팩터라 단계별 테스트로 안전 진행 | 김현규 | Approved |
| DEC-029 | 2026-09-12 | **docs/processors/ 제조사별 사양 문서 + Source-of-Truth 정책 신설(이사 지침).** 구조 재편성 중 "문서 먼저" 채택. 제조사별 md(analog-way midra·alta·livepremier, novastar h-series, colorlight x100-pro·universe)를 Verified/UNKNOWN/Important Rules/Official Sources 형식으로 작성. SOURCE-OF-TRUTH.md에 데이터 정책(공식자료 우선순위, 개념 자동변환 금지, null≠false, 제조사별 processing model, 사양/limit 분리, 출처 기록, 2단계 판정, regression 고정, 작업 순서) 명문화. 코드 3분할(processor-data/limits/validator)은 후속 예정 — 현재는 processors.js(데이터)+engine.js(로직) 유지. 문서만 추가(코드·화면 무변경) | 사양 추정 방지·유지보수성. 코드 분리 전 위험 0인 문서부터 | 김현규 | Approved |
| DEC-028 | 2026-09-12 | **X100 Pro 공식 "Max. layers" 반영.** X100 Pro Spec V2.0의 "Max. layers"(2U 32·4U 32·7U 64)를 **해상도 무관 총 레이어 `maxLayers`** 로 독립 저장(global4k/2k로 쪼개지 않음, 이사 지침). engine: global_window 모델의 레이어 검사를 per-resolution(4K/2K) 대신 **총 레이어(≤maxLayers)** 단일 검사로 변경('최대 레이어' 항목, fallback maxWindows). → X100 Pro 레이어 판정 가능해짐(윈도우≠레이어 원칙 유지). 7U I/O 확인(입력보드 8·8×4K/32×2K, 출력보드 8·8×4K/32×2K) | 공식 표기 그대로 총 레이어로 저장, 해상도별 자원관계 미확인이므로 분할 금지 | 김현규 | Approved |
| DEC-027 | 2026-09-12 | **Colorlight 공식 출력값 반영(이사 사이트 직접 확인).** X100 Pro: 출력보드 2U 4·4U 4·7U 8장, 4K 출력 4·4·8, 2K 출력 16·16·32(4K/2K 출력보드 혼용 불가 명시, 동시 사용 아님). 전역 레이어는 여전히 null(윈도우≠레이어). U15 Max: 입력보드 30·독립 60×4K/120×2K, 출력보드 20·40×4K/120×2K, 전역 80×4K/320×2K(공식 V1.0). 보드당 레이어는 공식 미확인 → null 유지(partial_official). 슬롯 40은 입출력 보드 합과 별개로 관리 | 이사 공식 사이트 직접 확인값. 추정 없이 공식값만, per-board 미확인은 null | 김현규 | Approved |
| DEC-026 | 2026-09-12 | **handoff v2(공식 확인 결과) 기능/상태 반영.** 공식 확인값만 교체, UNKNOWN은 null 유지("없다는 문구 못 찾음"=X 아님). NovaStar H: Seamless·Fade·Genlock·HDR·10bit=O, True A/B·PVW/PGM·AMX/Crestron=null. X100 Pro: Genlock·10bit(출력보드)=O, monitoringPreview=O, HDR·Seamless·Fade·True A/B·AMX/Crestron=null. Universe(U6/U9/U15): Fade·HDR·10bit·monitoringPreview=O, Seamless·True A/B·Genlock=null(U6 trueAB false→null로 정정), U15 10bit 추가. Aquilon RS: Genlock·True A/B·AMX·Crestron=O. Alta Zenith 200: Genlock·Crestron=O, AMX=null. Midra Pulse/Eikos: 믹싱2/분할4 official 승격. **신규 필드 `monitoringPreview`(≠previewProgram) 추가.** | 공식 근거 있는 값만 확정, 모니터링과 A/B PVW/PGM 구분. 리셀러/추정 배제 | 김현규 | Approved |
| DEC-025 | 2026-09-12 | **handoff 문서(processor_selector_claude_handoff.md) 확정값 반영.** (1) Midra Pulse 4K·Eikos 4K: 믹싱 4K 2 / 분할 4K 4(프로젝트 조사 기준, partial_official). (2) Universe U9 Max: 공식 V1.1 값 — 입력보드 18·독립 36×4K/108×2K, 출력보드 10·20×4K/60×2K, 보드당 4×4K/16×2K → status official. (3) engine: 입력 슬롯 공유검사(4K1/2K4 비율)를 global_window·per_output_card(X100·NovaStar)로 한정 — Universe(screen_group)는 입력 밀도가 달라(U9 보드당 2×4K) 제외. 검증: IF015R 12×6=7680×2160→필요 4K 출력 2(§10 일치). **보류: Eikos Wide Canvas(§2·§10), 제조사별 UI 표시(§8) — 별도 PR.** HDMI2.0 입·출력 가정(DEC-023/024)은 유지하되 화면에 '가정' 라벨로 표기(handoff §11 추정금지와 상충하지 않게 명시) | 공식 확정값 우선 반영, 미확인은 null 유지. Universe 입력 밀도 차이 정확 반영 | 김현규 | Approved |
| DEC-024 | 2026-09-12 | **입력카드 "슬롯 1개 = 4K 1채널 또는 2K 4채널" 모델(이사 확인).** NovaStar H·Colorlight X100 Pro의 4K 입력 카드는 카드당 4K 1채널이고, 입력 슬롯은 2K/4K 카드를 필요에 따라 혼용(X100 Pro 실측: 슬롯당 4K 1·2K 4와 정확히 일치). engine `inputsCapacity()`: 공식 maxIndependent4k/2k 있으면 그대로(X100), 없고 maxInputBoards 있으면 max4k=슬롯·max2k=슬롯×4로 유도(가정). 4K·2K 동시 요구 시 '입력 슬롯' 공유 예산 검사 추가(필요 4K + ⌈필요 2K/4⌉ ≤ 슬롯). INPUT_2K_PER_CARD=4. → NovaStar H2~H20 독립 4K 입력 = 입력카드 수(4·10·15·15·30·30·40), 2K = ×4. 데이터는 그대로(유도는 엔진에서) | 입력 슬롯 유연 구성을 정확히 반영. 추정은 엔진 계산에만, 데이터 원본은 정직 유지 | 김현규 | Approved |
| DEC-023 | 2026-09-12 | **출력카드 HDMI 2.0 가정(이사 지침).** NovaStar·Colorlight의 4K 출력 수를 "출력카드는 모두 HDMI 2.0(4K@60)을 쓴다" 가정으로 산출: 공식 `outputs.max4k`가 있으면 그대로(U6 Max=10 등), 없고 per_output_card/screen_group이면 **카드당 4K(perOutputCard4k/perBoard4k) × 출력보드 수**로 유도(assumed=true, 라벨 '4K 출력(HDMI2.0 가정)'). engine `outputs4kCapacity()` 신설, validateProcessor 4K 출력 검사가 이를 사용. 데이터(processors.js)의 max4k는 미확인이면 그대로 null 유지(추정값은 엔진에서만) → NovaStar H2~H20 4K 출력 = 8·12·20·40·40·64·80(유도). X100 Pro는 출력보드 수 미상이라 유도 불가(확인 필요 유지) | 실무상 노바/컬러 출력을 HDMI 2.0 4K@60 단위로 통일해 필요 출력=포트 수로 비교. 데이터 원본은 정직하게 null 유지, 가정은 엔진 계산에만 반영 | 김현규 | Approved |
| DEC-022 | 2026-09-12 | **Analog Way 목록 정리(이사 지침).** (1) Midra QuickVu 4K·QuickMatrix 4K **단종 → 삭제**(Pulse 4K·Eikos 4K만 유지). (2) **소형 작업(필요 4K 출력 ≤2)에서 Aquilon(RS) 전 모델을 추천 목록에서 숨김** — 가격이 비싸 소형에 부적합, 예외 없음(방송급 요구여도 소형이면 숨김). `rankProcessors`에서 필터, 하드제약 로직 자체는 불변. (3) "NovaStar/Colorlight 레이어 ≠ AW 믹싱 레이어" 원칙 유지 확인 | 실무상 2×4K 출력에 Aquilon은 과사양·고가. 단종 모델 노출 방지 | 김현규 | Approved |
| DEC-021 | 2026-09-12 | **레이어 판정 정교화(자료문서 §19~27), 1차 엔진+테스트.** `validateOutputCardLayers` 확장: (1) `perOutputCardDemand`를 **카드별 배열**로 받아 각 카드 2K 환산 예산·사용 카드 수 검사(경계 걸침=여러 카드에 중복 배치→cross-output 지원, 문서 §19), (2) 배치 정보 없으면 **이론적 분산 가능성**만 보고 "배치 확인 필요" note(문서 §21), per-board 사양 없으면 ok=null. §26 회귀테스트 6종 반영(노바 몰림 FAIL/분산 PASS, X100 복제 8→OK·9→FAIL, U6 보드 FAIL/분산 PASS). 화면(제조사별 의미·이유 표시 §24·§25)은 2차. 등급 변화 없음(U6 적합 유지, 노바 조건부 유지) | 세 제조사 레이어 개념을 단일 maxLayers로 통일하지 않음(문서 §23). Capacity Fit + Operation Fit 분리 유지(§27). 정밀 카드/보드 배치는 배치 입력 필요(P5-4) | 김현규 | Approved |
| DEC-020 | 2026-09-12 | **레이어 경계 걸침(cross-output) 규칙 기록(구현 보류).** NovaStar H는 레이어가 출력카드 경계에 걸치면 양쪽 카드에서 레이어 예산을 각각 소모(배치 위치가 실효 용량을 좌우) — Colorlight(X100 Pro/Universe)는 전역 풀이라 이중 소모 없음(이사 김현규 확인). 지금은 SPEC §7.3 규칙 6·§7.4에 **기록만** 하고, 자동 계산(레이어 배치도 입력)은 후속(P5-4). 화면/코드 변경 없음 | 정확한 도메인 규칙 보존. 배치도 입력은 화면 복잡도가 커서 이사 지침대로 기록 우선 | 김현규 | Approved(기록만) |
| DEC-019 | 2026-09-12 | **프로세서 DB 갱신(Colorlight/NovaStar) + 출력카드 레이어 예산 로직.** 이사(김현규) 제공 자료문서(Colorlight_NovaStar_Claude_Code_Data.md) 반영. NovaStar H: 4→7모델(H2·H5·H9·H9E·H15·H15E·H20), **출력카드 수 정정(H5 10→3·H9 15→5; 기존값은 입력카드 수 오기)**, 카드당 16×2K/8×DL/4×4K, 입력카드 수 추가(독립 4K 입력 수는 카드 의존→null 유지), status official. Colorlight U6 Max: 전역 4K 20·per-board 16×2K/4×4K·cut·trueAB=false 추가(official). U9/U15 Max: 전역 레이어 공식 반영, status **partial_official**(신규 상태값). X100 Pro: 문서와 일치(변경 없음). engine: **출력카드 2K 환산 예산**(4K=4·DL=2·2K=1, 카드 16) `outputCardUsage2kEq`·`validateOutputCardLayers` 강화(배치 정보 있을 때만 카드별 검사, 없으면 전역 레이어로 갈음), screen_group per-board 필드명 `perBoard` 통일 | 문서 official/partial_official 우선, null 추정 금지(§17). NovaStar는 전역 레이어만으로 PASS 금지→카드 예산 검사 필요(§4·§16). 독립입력≠윈도우≠레이어≠출력 유지 | 김현규 | Approved |
| DEC-018 | 2026-09-12 | **Video Processor Selector 1차(엔진+데이터+테스트) 착수·승인.** 오너 컨텍스트 문서(Samsung_LED_Processor_Selector_Claude_Context.md) 스키마·규칙 채택: (1) `src/processors.js` 순수 데이터(문서 §9 스키마), (2) `engine.js`에 `processorRequirements`·`validateProcessor`·`validateOutputCardLayers`·`rankProcessors` 추가, (3) 2단계 판정(하드제약 PASS/CONDITIONAL/FAIL → 등급 권장/적합/조건부/한계/부적합, **근거 표시**). 확정 규칙: `required4kOutputs=⌈resW/3840⌉×⌈resH/2160⌉`; **독립입력 ≠ 레이어 ≠ 윈도우 ≠ 출력**(문서 §4); True A/B는 믹싱 레이어만 사용(분할 대체 불가); NovaStar는 출력카드별 레이어 검증; **S-Box 이중화 ≠ 프로세서 출력 이중화**(문서 §13). 데이터: Analog Way(Midra I/O·Aquilon RS 믹싱/분할) 및 오너 문서 표(X100 Pro 2U/4U/7U·U6 Max·Alta Zenith 200)만 채우고 **미확인 값은 전부 null + needs_verification**(문서 §15, PDF 사양서 이 환경 열람 불가). 화면 변화 없음(2차 PR에서 '05 비디오 프로세서' UI 추가) | 문서 §16 우선순위대로 "정확한 PASS/FAIL 판정" 먼저. 추정 스펙 금지(CLAUDE.md 규칙 2). 화면 무변경으로 디자인 작업과 충돌 회피 | 김현규 | Approved |

## Approval Log

| Timestamp | Stage or Artifact | Requested From | Result | Notes |
|---|---|---|---|---|
| 2026-09-16 | 3D 렌더 기능 방식 선택(별도 3D 뷰어 모듈) + 공간 깊이 입력 추가 | 김현규 | Approved | A안(기존 뷰 회전)·C안(WebGL) 대신 B안 채택. 이후 오너 레퍼런스 이미지에 따라 자유 회전 → 큐브 시점 + 공간 타입/가구 프리셋으로 방향 조정 |
| 2026-07-23 | 브라우저 접속(삼성 도구) | 김현규 | Approved | Browser 1 선택 |
| 2026-07-23 | 프로토타입 v0.1 | 김현규 | Pending | 리뷰 대기 |
| 2026-07-23 | SPEC.md 확정(예정) | 김현규 | Pending | Construction 착수 게이트 |
| 2026-09-12 | Video Processor Selector 계획(6항목) + 진행 방식(2 PR 분할, 데이터=문서표+확실값) | 김현규 | Approved | 1차: 엔진+데이터+테스트(화면 무변경), 2차: 05 UI |

## Verification Log

| Timestamp | Scope | Method | Result | Evidence |
|---|---|---|---|---|
| 2026-09-16 | 3D 뷰 Three.js 전환 STEP 1(골격) | node:test + 헤드리스 브라우저(SwiftShader WebGL) | Pass | 273/273 통과(신규 10 — `tests/gl-model.test.js`). 브라우저 검증: 정면 뷰 정상·복귀 정상, 3D 뷰에서 WebGL 컨텍스트 생성·씬 출력 확인, 씬에 들어간 값이 engine·room-presets 결과와 일치(공간 10×3.5×10 m, LED 9.6×2.16 m, 여백 0.2 m, 하단 높이 1.0 m, 캐비닛 깊이 79.5 mm, 배열 10×4, 무대 10×2.6×0.28 m) — 11개 항목 전부 PASS. 회의실·강의실·소강당·대강당 렌더, OrbitControls 드래그, '맞춤' 복귀, 뷰포트 1512/1100/820 px 리사이즈 대응 확인. 콘솔 오류 없음(`prices.local.js` 404는 가격표 미동봉으로 인한 기존 동작 — try/catch 처리됨). `renderPreview()` 327줄이 main과 바이트 단위로 동일함을 diff로 확인 |
| 2026-09-16 | 3D 뷰 미세 보정 4건(카메라 높이·포인트 벽 고정·LED framing·캔버스 높이) | node:test + 헤드리스 브라우저 렌더 | Pass | 263/263 통과(신규 4). 오너 지정 시나리오(소강당 10.0 × 3.5 × 10.0 m, IF015R-M 4×4)로 검증: 5개 투시 시점 카메라 높이 전부 2,490 mm(이전 3,368 mm), LED 시각 크기 +14.0%이면서 네 모서리 화면 안, 좌측→정면→우측 시점 변경에도 포인트 벽은 왼쪽 벽 고정(이전에는 좌↔우 이동), '맞춤' 후 화면이 픽셀 단위로 동일, 캔버스 1600px=640·1440px=634·1024px=451·430px=275 px, PNG 저장(`3D_IF015R-M_회의실.png`) 정상, 정면 뷰 렌더 무변화 |
| 2026-09-16 | 3D 뷰 비주얼 개선(카메라·팔레트·LED·치수·가구) | node:test + 헤드리스 브라우저 렌더 | Pass | 259/259 통과(신규 2). 6개 시점 전부에서 방 모서리가 카메라 앞에 있고 화면 안에 들어옴을 검증. 회의실·강의실·중강당·상황실·평면도 렌더와 정면 뷰 나란히 비교, PNG 저장(파일명 포함) 확인, 콘솔 에러 없음 |
| 2026-09-16 | 3D 뷰 수정 4건 + 사람 배치 | node:test + 헤드리스 브라우저 렌더 | Pass | 255/255 통과(신규 10). 의자 방향 불변조건은 공간 타입·테이블 모양·좌석수 100여 조합 전수 검사. 브라우저에서 보트·원형·U자·상황실·강당·평면도 렌더 및 인물 교체·사람 끄기 확인, 콘솔 에러 없음 |
| 2026-09-16 | 3D 미리보기(투영·컷어웨이·가구 배치·도형) | node:test + 헤드리스 브라우저 렌더 | Pass | 245/245 통과(기존 193 + 신규 52). 공간 타입 6종 × 방 크기 3종 배치가 모두 방 안에 들어옴을 검증. 브라우저에서 회의실·강의실·중강당·상황실·평면도·정면 렌더 및 PNG(3배) 저장 확인 |
| 2026-07-23 | 계산 엔진(충진·크기·해상도·중량·전력) | Node 수동 실행 | Pass | 세션 실행 로그 |
| 2026-07-23 | 삼성 도구 대조(MP012F / 6×3.4m) | 브라우저 화면 수기 대조 | Partial | 산출값 일치, 세로 행수 불일치(6 vs 7) |
| 2026-09-12 | 프로세서 선정 엔진(요구산출·PASS/FAIL·카드별 레이어·랭킹) | node:test (`tests/processor.test.js`) | Pass | 87/87 통과(기존 71 + 신규 16). 문서 §12 케이스(출력량·X100 입력 8/9·A/B 믹싱·NovaStar 카드한계·미확인=CONDITIONAL) 포함 |
| 2026-09-12 | 프로세서 DB 갱신 + 카드 예산 로직(DEC-019) | node:test + 헤드리스 렌더 | Pass | 93/93 통과. TEST3~8(U6 20/21·카드 4/5·2K환산 16/17) 반영. 화면 제품 23개(NovaStar 7 포함), U6 Max 권장 승격 확인 |
| 2026-09-12 | 레이어 판정 정교화 §19~27(DEC-021) | node:test + 헤드리스 렌더 | Pass | 97/97 통과. §26 6종(카드 배열 몰림/분산, X100 복제, U6 보드) 반영. v171: 노바/유니버스에 '카드/보드별 배치' 줄 표시, 등급 불변(7 권장·16 조건부) |
